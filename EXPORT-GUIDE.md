# 📦 Как экспортировать и разместить на своём хостинге

## 🎯 Зачем это нужно

Ваш проект сейчас работает на **shmelidze.online** (хостинг poehali.dev), но вы можете:
- ✅ Разместить на своём домене
- ✅ Использовать свой хостинг (дешевле)
- ✅ Получить полный контроль над сайтом
- ✅ Убрать технические скрипты платформы

## 🚀 Способ 1: Через скрипты (самый простой)

### На Linux/macOS:
```bash
chmod +x build-production.sh
./build-production.sh
```

### На Windows:
Дважды кликните на файл `build-production.bat`

**Результат:** В папке `dist/` появится чистый билд без скриптов poehali.dev

## 📥 Способ 2: Через интерфейс poehali.dev

1. В редакторе нажмите **Скачать → Скачать билд**
2. Получите ZIP-архив с готовым сайтом
3. Распакуйте на свой хостинг

## 🌐 Куда загружать билд

### Вариант A: Обычный хостинг (Shared Hosting)

**Популярные хостинги:**
- beget.com
- timeweb.com
- reg.ru
- hosting.ru

**Инструкция:**
1. Подключитесь по FTP (FileZilla, WinSCP)
2. Загрузите содержимое папки `dist/` в `public_html/` или `www/`
3. Создайте файл `.htaccess` со следующим содержимым:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

4. Готово! Откройте ваш домен

### Вариант B: Netlify (бесплатно)

1. Зайдите на [netlify.com](https://netlify.com)
2. Зарегистрируйтесь (можно через GitHub)
3. Перетащите папку `dist/` в окно браузера
4. Получите бесплатный домен `название.netlify.app`
5. По желанию привяжите свой домен

### Вариант C: Vercel (бесплатно)

1. Установите Vercel CLI:
```bash
npm install -g vercel
```

2. Запустите из папки проекта:
```bash
vercel --prod
```

3. Следуйте инструкциям в терминале
4. Получите домен `название.vercel.app`

### Вариант D: GitHub Pages (бесплатно)

1. Создайте репозиторий на GitHub
2. Загрузите содержимое `dist/` в ветку `gh-pages`
3. В настройках репозитория включите GitHub Pages
4. Сайт будет доступен по адресу `username.github.io/repo-name`

## ⚙️ Важная настройка: SPA Routing

Ваш сайт — это Single Page Application (SPA). Все маршруты обрабатывает React Router.

**Проблема:** При прямом заходе на `/writeoff` или `/analytics` сервер вернёт 404.

**Решение:** Настроить редиректы - все запросы должны возвращать `index.html`.

### Nginx
```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

### Apache
```apache
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

### Netlify
Создайте файл `_redirects` в `dist/`:
```
/*  /index.html  200
```

### Vercel
Создайте `vercel.json`:
```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

## 🔍 Проверка

После размещения откройте консоль браузера (F12) и убедитесь:

✅ **Должно быть:**
- Загружается `index.html`
- Загружаются файлы из `/assets/`
- Приложение работает

❌ **НЕ должно быть:**
- Ошибок `cover is not defined`
- Загрузок `pp-min-2.js`, `telemetry-min.js`
- CORS ошибок от `api.poehali.dev`
- 404 ошибок на маршруты

## 🗄️ А что с базой данных?

**Вариант 1: Продолжать использовать poehali.dev (рекомендуется)**
- Backend функции остаются на poehali.dev
- Фронтенд обращается к ним по API
- База данных работает как и раньше
- **Ничего менять не нужно!**

**Вариант 2: Перенести всё на свой сервер**
Потребуется:
1. Установить PostgreSQL
2. Создать таблицы (см. `db_migrations/`)
3. Развернуть backend функции на своём сервере
4. Изменить URL API в коде фронтенда

## 💰 Стоимость хостинга

| Вариант | Цена | Что входит |
|---------|------|------------|
| Netlify (Free) | 0₽ | 100GB трафика/мес, SSL, CDN |
| Vercel (Free) | 0₽ | 100GB трафика/мес, SSL, CDN |
| GitHub Pages | 0₽ | Неограниченный трафик, SSL |
| Shared Hosting | 100-300₽/мес | Домен, почта, FTP |
| poehali.dev | по тарифу | База данных, backend, хостинг |

## 🆘 Частые проблемы

**Белый экран после загрузки**
→ Проверьте настройку SPA роутинга (см. выше)

**404 на файлы из /assets/**
→ Убедитесь, что загрузили всю папку `dist/` целиком

**Не работает авторизация**
→ Функция `/backend/users/` должна остаться на poehali.dev или перенесена на свой сервер

**Не сохраняются данные**
→ База данных находится на poehali.dev, проверьте доступ к API

## 📞 Нужна помощь?

1. Проверьте [PRODUCTION-BUILD.md](PRODUCTION-BUILD.md) для детальной информации
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Напишите в поддержку вашего хостинга

---

**Совет:** Начните с Netlify или Vercel — это бесплатно и занимает 2 минуты!
